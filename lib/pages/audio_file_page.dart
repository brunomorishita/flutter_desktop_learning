import 'package:fluent_ui/fluent_ui.dart';

class AudioFilePage extends StatelessWidget {

  // This widget is the root of your application.
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("It's AudioFile Page"),
    );
  }

}